name: flutter_directory_tree
description: Desktop-friendly virtualized tree view with keyboard navigation, tri-state selection, and multi-root support.
version: 1.0.0
homepage: https://github.com/omar-hanafy/flutter_directory_tree
repository: https://github.com/omar-hanafy/flutter_directory_tree
issue_tracker: https://github.com/omar-hanafy/flutter_directory_tree/issues
documentation: https://github.com/omar-hanafy/flutter_directory_tree#readme

topics:
  - filesystem
  - tree-view
  - desktop
  - file-explorer

environment:
  sdk: ^3.2.0
  flutter: ">=3.22.0"

dependencies:
  directory_tree: ^1.0.0
  flutter:
    sdk: flutter
  path: ^1.9.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
